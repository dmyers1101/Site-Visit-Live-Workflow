# GCP research notes

Use this folder to capture current research and library/version confirmations for Google Cloud tasks.

Before wiring a new GCP product or API, document:
- the date
- source URLs
- library and version in use
- key caveats or flags

When a deployment fails or a library changes, refresh the note rather than relying on older assumptions.
